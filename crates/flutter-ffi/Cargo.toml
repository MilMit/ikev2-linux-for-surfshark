[package]
name = "milmit-vpn-flutter-ffi"
version = "0.4.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
surfshark-ikev2-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
once_cell = "1"
hex = "0.4"
